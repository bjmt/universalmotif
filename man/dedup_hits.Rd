% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dedup_hits.R
\name{dedup_hits}
\alias{dedup_hits}
\title{Deduplicate overlapping motif hits.}
\usage{
dedup_hits(hits, by = "pvalue", reverse = FALSE, ignore.strand = FALSE,
  ignore.motif = FALSE, seq = "sequence", motif = "motif",
  strand = "strand")
}
\arguments{
\item{hits}{\code{data.frame} or \code{GRanges}. For a \code{data.frame}, must have
columns \code{start}, \code{end}, and the columns named by \code{by}, \code{seq},
\code{motif}, \code{strand}. For a \code{GRanges}, the \code{seqnames}/\code{start}/\code{end}/
\code{strand} slots are used; the \code{motif} and priority columns are read
from \code{mcols()}.}

\item{by}{\code{character(1)}. Name of the priority column. The semantic
is auto-detected: a column named \code{pvalue} (or anything starting
with \code{pval}) is treated as lower-is-better; anything else (e.g.
\code{score}) is treated as higher-is-better. Use \code{reverse = TRUE} to
flip.}

\item{reverse}{\code{logical(1)}. Flip the priority direction.}

\item{ignore.strand}{\code{logical(1)}. If \code{TRUE}, \code{+} and \code{-} hits
compete with each other at overlapping coordinates.}

\item{ignore.motif}{\code{logical(1)}. If \code{TRUE}, hits from different
motifs compete with each other at overlapping coordinates.}

\item{seq}{\code{character(1)}. Name of the sequence-id column in a
\code{data.frame} input. Default \code{"sequence"}. Ignored for \code{GRanges}.}

\item{motif}{\code{character(1)}. Name of the motif-id column in a
\code{data.frame} input. Default \code{"motif"}. Ignored for \code{GRanges}.}

\item{strand}{\code{character(1)}. Name of the strand column in a
\code{data.frame} input. Default \code{"strand"}. Ignored for \code{GRanges}.}
}
\value{
The input \code{hits}, subsetted to the kept rows in the original
order.
}
\description{
Given a set of motif hits, typically the output of
\code{\link[=scan_sequences]{scan_sequences()}} or \code{\link[=scan_sequences_lite]{scan_sequences_lite()}}, or a \code{GRanges} /
\code{data.frame} from any other source, drop overlapping hits within
each \verb{(sequence, motif, strand)} group using a greedy
algorithm: cluster overlapping hits, then within each cluster keep
the hit with the best priority and, after that, any other hit that
does not overlap an already-kept hit.
}
\details{
Note that this differs from \code{\link[=scan_sequences]{scan_sequences()}}'s built-in
\code{no.overlaps} argument, which collapses every connected-overlap
cluster to a single survivor (hierarchical clustering with
\code{cutree}). The greedy approach used here keeps legitimately distinct
hits even when an intermediate noisy hit bridges them, which is
usually what users want.

Coordinate convention: rows must have \code{start <= end}. Overlaps are
inclusive: two hits overlap if \code{a$start <= b$end & b$start <= a$end}.
}
\examples{
library(universalmotif)
motifs <- list(create_motif("TATAAA", name = "M1"),
               create_motif("CACGTG", name = "M2"))
seqs <- create_sequences(seqnum = 5, seqlen = 400, rng.seed = 1)
hits <- scan_sequences_lite(motifs, seqs, pvalue = 5e-2,
                        return.granges = FALSE)
deduped <- dedup_hits(hits)
nrow(hits)
nrow(deduped)

}
\seealso{
\code{\link[=scan_sequences_lite]{scan_sequences_lite()}}, \code{\link[=scan_sequences]{scan_sequences()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
