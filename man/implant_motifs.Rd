% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/implant_motifs.R
\name{implant_motifs}
\alias{implant_motifs}
\title{Implant sampled motif instances into sequences at known positions.}
\usage{
implant_motifs(motifs, sequences, n.per.seq = 1L, rate = NULL,
  positions = NULL, strand = c("both", "+"), centre.bias = 1L,
  min.spacing = 0L, max.retries = 100L, return.indices = FALSE)
}
\arguments{
\item{motifs}{See \code{\link[=convert_motifs]{convert_motifs()}} for accepted motif formats.
DNA or RNA only, amino-acid and custom alphabet motifs are
rejected. A single motif or a list of motifs.}

\item{sequences}{\code{XStringSet}. Host sequences to plant into. Must
share the motif alphabet.}

\item{n.per.seq}{\code{integer(1)}. Mode A. Number of instances to plant
in each sequence. Default \code{1L}.}

\item{rate}{\code{numeric(1)} or \code{NULL}. Mode B. Per-bp Poisson rate.
If non-\code{NULL}, overrides \code{n.per.seq}. Default \code{NULL}.}

\item{positions}{\code{list} or \code{NULL}. Mode C. List of length
\code{length(sequences)}; element \code{i} is an integer vector of 1-based
start positions. Overrides \code{n.per.seq} and \code{rate}. Default \code{NULL}.}

\item{strand}{\code{"both"} or \code{"+"}. Whether to also draw from the
reverse complement. Default \code{"both"}.}

\item{centre.bias}{\code{integer(1)}. Irwin-Hall N. \code{1L} (default) is
uniform; larger values cluster implants near the centre of each
sequence. Ignored in \code{positions} mode.}

\item{min.spacing}{\code{integer(1)}. Minimum gap (in bp) between the
end of one implant and the start of the next within the same
sequence. \code{0L} (default) allows abutting but not overlapping
implants. Ignored in \code{positions} mode.}

\item{max.retries}{\code{integer(1)}. Per-implant cap on placement
attempts before giving up (with a warning at the end if any
targets fell short). Default \code{100L}.}

\item{return.indices}{\code{logical(1)}. If \code{FALSE} (default), return
only the modified \code{XStringSet}. If \code{TRUE}, return a \code{list} with
the modified sequences plus a \code{data.frame} of per-implant
ground-truth metadata.}
}
\value{
If \code{return.indices = FALSE}: an \code{XStringSet} of the same
length, width, and class as \code{sequences}, with bases overwritten
at the implant positions. Names are preserved. If
\code{return.indices = TRUE}: a \code{list} with two elements --
\code{sequences} (the modified \code{XStringSet}, as above) and \code{indices}
(a \code{data.frame} with columns \code{sequence.i} (1-based),
\code{motif.i} (1-based index into \code{motifs}), \code{start} (1-based),
\code{width}, \code{strand} (\code{"+"} / \code{"-"}), and \code{planted} (the actual
implanted string, already reverse-complemented when
\code{strand == "-"})).
}
\description{
\code{\link[=implant_motifs]{implant_motifs()}} overwrites bases in an existing \code{XStringSet} with
samples drawn column-by-column from one or more motif PPMs at chosen
positions, returning the modified sequences together (optionally)
with a ground-truth data.frame of where each implant was placed.
This produces benchmark-grade positive sequences for testing
\code{\link[=scan_sequences2]{scan_sequences2()}}, \code{\link[=motif_finder]{motif_finder()}}, \code{\link[=enrich_motifs2]{enrich_motifs2()}},
\code{\link[=motif_peaks]{motif_peaks()}}, or any other discovery / scanning pipeline against
a known answer.
}
\details{
Three insertion modes (mutually exclusive; later non-\code{NULL} argument
wins):
\enumerate{
\item Fixed count (\code{n.per.seq}): plant exactly \code{n.per.seq}
instances in every sequence. Matches HOMER-style ground-truth
simulations.
\item Poisson rate (\code{rate}): per-sequence count drawn from
\code{rpois(1, rate * width(seq))}. Density scales naturally with
sequence length.
\item Explicit positions (\code{positions}): a \code{list} of length
\code{length(sequences)}; element \code{i} is a 1-based integer vector of
start positions for that sequence. Bypasses \code{centre.bias},
\code{min.spacing}, and \code{max.retries}.
}

For each implant the function (a) picks a motif uniformly at random
from \code{motifs}, (b) chooses a strand (uniform \code{+}/\code{-} if
\code{strand = "both"}), (c) picks a start position (uniform or centre-
biased via Irwin-Hall, retried up to \code{max.retries} times if it
collides with an already-placed implant within \code{min.spacing}),
(d) samples one instance from the motif's PPM column-by-column,
reverse-complements if needed, and (e) overwrites that span in the
target sequence.
}
\examples{
\dontrun{
library(universalmotif)
data(ArabidopsisMotif)
seqs  <- create_sequences("DNA", seqnum = 100, seqlen = 500)
set.seed(1)
res   <- implant_motifs(ArabidopsisMotif, seqs, n.per.seq = 1,
                        return.indices = TRUE)
out   <- res$sequences
truth <- res$indices
## Recover-rate check
hits <- scan_sequences(ArabidopsisMotif, out, threshold = 0.85,
                       threshold.type = "logodds")
## Density check
peaks <- motif_peaks(out, motif = ArabidopsisMotif)
plot_motif_peaks(peaks)
}

}
\seealso{
\code{\link[=sample_sites]{sample_sites()}}, \code{\link[=create_sequences]{create_sequences()}}, \code{\link[=scan_sequences2]{scan_sequences2()}},
\code{\link[=motif_finder]{motif_finder()}}, \code{\link[=motif_peaks]{motif_peaks()}}, \code{\link[=enrich_motifs2]{enrich_motifs2()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
