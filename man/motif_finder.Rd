% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motif_finder.R
\name{motif_finder}
\alias{motif_finder}
\title{Discover \emph{de novo} motifs in a set of sequences.}
\usage{
motif_finder(sequences, bkg.sequences = NULL, min.width = 6L,
  max.width = 15L, nmotifs = 10L, hit.pvalue = 1e-04,
  stop.pvalue = 0.001, qvalue = 0.001, dedup.overlap = 0.5, RC = TRUE,
  shuffle.k = 2L, rng.seed = sample.int(1e+06, 1), pseudocount = 1L,
  nthreads = 1)
}
\arguments{
\item{sequences}{\code{XStringSet}. DNA or RNA target sequences.}

\item{bkg.sequences}{\code{XStringSet} or \code{NULL}. Background sequences.
If \code{NULL} (default), target sequences are shuffled k-let-conserving
via \code{\link[=shuffle_sequences]{shuffle_sequences()}} with \code{k = shuffle.k}, matching \verb{yamtk me}
default behaviour.}

\item{min.width}{\code{integer(1)}. Minimum motif width. Default \code{6L}.}

\item{max.width}{\code{integer(1)}. Maximum motif width. Default \code{15L}.
Hard ceiling 30.}

\item{nmotifs}{\code{integer(1)}. Maximum number of motifs to discover per
width before moving on. Default \code{10L}.}

\item{hit.pvalue}{\code{numeric(1)}. P-value threshold for scoring a single
hit during scanning. Default \code{1e-4}.}

\item{stop.pvalue}{\code{numeric(1)}. Per-motif Fisher's exact p-value below
which a candidate motif is accepted. Default \code{1e-3}.}

\item{qvalue}{\code{numeric(1)}. Final BH-adjusted q-value cutoff. Motifs
with \code{qvalue} above this are dropped. Default \code{1e-3}.}

\item{dedup.overlap}{\code{numeric(1)}. Cross-width dedup overlap fraction.
When the intersection of two motifs' positional coverage divided by
the smaller motif's coverage exceeds this, the higher-p-value motif
is dropped. Default \code{0.5}.}

\item{RC}{\code{logical(1)}. If \code{TRUE} (default), scan both strands.}

\item{shuffle.k}{\code{integer(1)}. K-let size for the background shuffle
(only used when \code{bkg.sequences = NULL}). Default \code{2L}.}

\item{rng.seed}{\code{integer(1)}. RNG seed for background shuffling.
Default \code{sample.int(1e6, 1)}; set explicitly for reproducible runs.}

\item{pseudocount}{\code{integer(1)}. Pseudocount used when converting
per-position counts to log-odds scores. Default \code{1L}.}

\item{nthreads}{\code{numeric(1)}. Number of threads. Parallelism is at
the width level. \code{nthreads = 0} uses all available threads.}
}
\value{
A \code{universalmotif_df} (see \code{\link[=to_df]{to_df()}}) with one row per
discovered motif, sorted by \code{pvalue} ascending. The standard
\code{to_df()} columns (\code{name}, \code{altname}, \code{family}, \code{organism},
\code{consensus}, \code{alphabet}, \code{type}, \code{nsites}, \code{pval}, \code{eval}, \code{motif},
...) are populated from each discovered motif. The yamtk-specific
stats are carried as additional columns:

\itemize{
\item \code{rank}: 1-based discovery rank
\item \code{width}: motif width (post IC-trim)
\item \code{seqs_pos}: positive sequences with >=1 hit
\item \code{seqs_neg}: background sequences with >=1 hit
\item \code{sites_pos}: total target hits
\item \code{sites_neg}: total background hits
\item \code{n_pos}: number of positive sequences
\item \code{n_neg}: number of background sequences
\item \code{pvalue}: Fisher's exact one-sided p-value
\item \code{qvalue}: Benjamini-Hochberg q-value
}

Use \code{\link[=to_list]{to_list()}} on the returned object to recover a plain list of
\code{universalmotif} S4 objects.
}
\description{
\code{motif_finder()} is a minimalist \emph{de novo} motif discovery function,
whose defaults mirror the command-line tool
\href{https://github.com/bjmt/yamtk}{yamtk}. The pipeline works through a
user-controlled range of motif widths; at each width it enumerates the
over-represented k-mer seeds (using a Fisher's exact test on per-sequence
presence against a background set), aligns the Hamming-1 neighbours of
the best seed to form a PPM, refines that PPM over two re-scan passes,
accepts the motif if its Fisher's exact p-value passes \code{stop.pvalue}, and
then masks the covered positions and repeats, up to \code{nmotifs} times per
width. Once every width is done, the motifs are deduplicated across
widths by coverage overlap, BH-adjusted, IC-trimmed at the flanks, and
returned in p-value order.
}
\details{
Algorithm and defaults are a faithful port of
\href{https://github.com/bjmt/yamtk}{yamtk}, whose own design is in turn based
on the STREME algorithm (Bailey 2021): seed enumeration via word
counting with per-sequence Fisher's exact ranking, iterative PPM
refinement on positive sequences, per-motif Fisher's exact
significance against a shuffled or user-supplied background, and
position masking between discoveries.

Motifs with \code{qvalue > qvalue} are dropped from the result. To see
all discovered motifs regardless of significance, set
\code{qvalue = 1}.
}
\examples{
\dontrun{
library(universalmotif)
library(Biostrings)
set.seed(1)
planted <- DNAString("TTGACATA")
seqs <- create_sequences(seqnum = 100, seqlen = 200, rng.seed = 1)
## Plant the motif at random positions in 80\% of sequences:
for (i in seq_len(80)) {
  pos <- sample.int(200 - 8, 1)
  seqs[[i]][pos:(pos + 7)] <- planted
}
motifs <- motif_finder(seqs, qvalue = 1, rng.seed = 1)
to_list(motifs)[[1]]
}

}
\references{
Bailey TL (2021). "STREME: accurate and versatile sequence motif
discovery." \emph{Bioinformatics}, \strong{37}(18), 2834-2840.
\doi{10.1093/bioinformatics/btab203}.

Tremblay BJM (2026). yamtk: Yet Another Motif ToolKit.
\url{https://github.com/bjmt/yamtk}.
}
\seealso{
\code{\link[=scan_sequences_lite]{scan_sequences_lite()}}, \code{\link[=enrich_motifs_lite]{enrich_motifs_lite()}}, \code{\link[=shuffle_sequences]{shuffle_sequences()}},
\code{\link[=create_motif]{create_motif()}}, \code{\link[=to_df]{to_df()}}, \code{\link[=to_list]{to_list()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
