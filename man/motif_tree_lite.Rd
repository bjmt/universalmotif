% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motif_tree_lite.R
\name{motif_tree_lite}
\alias{motif_tree_lite}
\title{Generate ggtree-based motif trees using \code{compare_motifs_lite()}.}
\usage{
motif_tree_lite(motifs, layout = "rectangular", linecol = "family",
  labels = "none", tipsize = "none", legend = TRUE,
  branch.length = "none", min.overlap = 6, tryRC = TRUE, nthreads = 1,
  progress = FALSE, tiplogo = TRUE, tiplogo.align = TRUE,
  tiplogo.width = 1, tiplogo.height = 0.9, tiplogo.offset = NULL, ...)
}
\arguments{
\item{motifs}{\code{list} or \code{dist}. See \code{\link[=convert_motifs]{convert_motifs()}} for accepted
motif formats. Alternatively, a pre-built \code{dist} object (e.g. the
result of \code{as.dist((1 - compare_motifs_lite(...)) / 2)}) skips the
comparison step entirely and goes straight to tree construction.}

\item{layout}{\code{character(1)}. One of \code{c('rectangular', 'slanted', 'fan', 'circular', 'radial', 'equal_angle', 'daylight')}. Passed
through to \code{\link[ggtree:ggtree]{ggtree::ggtree()}}. Defaults to \code{"rectangular"} so that the
tip logos (see \code{tiplogo}) are visible; tip logos are only drawn on the
\code{"rectangular"} and \code{"slanted"} layouts.}

\item{linecol}{\code{character(1)}. Motif slot used to colour the
branches (e.g. \code{"family"}). Not used when \code{motifs} is a \code{dist}.}

\item{labels}{\code{character(1)}. Motif slot used to label the tips
(e.g. \code{"name"}). For \code{dist} input, only \code{"name"} is meaningful. Note:
drawing tip labels uses \code{ggtree::geom_tiplab(align = TRUE)}, which fails
to render under \pkg{ggplot2} >= 4.0 with \pkg{ggtree} <= 3.12 (ggtree's
internal \code{empty()} calls the removed \code{ggplot2:::is.waive()}); update
\pkg{ggtree} if you need tip labels. The tip logos (\code{tiplogo}) are
unaffected.}

\item{tipsize}{\code{character(1)}. Motif slot used to size the tips
(e.g. \code{"icscore"}). Not used when \code{motifs} is a \code{dist}.}

\item{legend}{\code{logical(1)}. Show legend for line colour and tip size.}

\item{branch.length}{\code{character(1)}. If \code{"none"}, draw a cladogram.
Passed through to \code{\link[ggtree:ggtree]{ggtree::ggtree()}}.}

\item{min.overlap}{\code{integer(1)}. Minimum overlap in columns for the
pairwise alignment to count. Forwarded to \code{\link[=compare_motifs_lite]{compare_motifs_lite()}}.
Default \code{6}.}

\item{tryRC}{\code{logical(1)}. Also test reverse-complement alignments.
Forwarded to \code{\link[=compare_motifs_lite]{compare_motifs_lite()}}. Default \code{TRUE}.}

\item{nthreads}{\code{numeric(1)}. Threads passed to \code{\link[=compare_motifs_lite]{compare_motifs_lite()}}.
\code{nthreads = 0} uses all available threads.}

\item{progress}{\code{logical(1)}. Print progress messages.}

\item{tiplogo}{\code{logical(1)}. Draw a sequence logo at each tip using
\code{\link[=geom_logo]{geom_logo()}}. On by default. Only drawn on the \code{"rectangular"} and
\code{"slanted"} layouts (other layouts warn and skip), and not available
for \code{dist} input. Default \code{TRUE}.}

\item{tiplogo.align}{\code{logical(1)}. Align the tip logos to a common,
zero-padded column frame (via \code{\link[=view_motifs_lite]{view_motifs_lite()}}) so that homologous
positions line up across tips. DNA/RNA only; falls back to unaligned
per-tip logos otherwise. Default \code{TRUE}.}

\item{tiplogo.width}{\code{numeric(1)}. Width of one motif position, in tree
x-axis units, for the tip logos. Default \code{1}.}

\item{tiplogo.height}{\code{numeric(1)}. Total height of a tip logo, in tree
y-axis units, centred on each tip. Default \code{0.9}.}

\item{tiplogo.offset}{\code{numeric(1)} or \code{NULL}. Gap between the tips and the
start of the logos, in tree x-axis units. If \code{NULL}, a small fraction of
the tree width is used. Default \code{NULL}.}

\item{...}{Additional arguments passed through to
\code{\link[ggtree:ggtree]{ggtree::ggtree()}}.}
}
\value{
A \code{ggplot} (\code{ggtree}) object.
}
\description{
\code{motif_tree_lite()} is the leaner counterpart of \code{\link[=motif_tree]{motif_tree()}}: it builds the
distance matrix via \code{\link[=compare_motifs_lite]{compare_motifs_lite()}} (mean Pearson correlation,
with built-in p-value / q-value machinery) instead of
\code{\link[=compare_motifs]{compare_motifs()}}. The distance for \code{hclust()} is derived from the
mean Pearson correlation matrix as \code{(1 - score) / 2}, mapping
similarity in \verb{[-1, 1]} to a symmetric distance in \verb{[0, 1]}. The
tree-construction and rendering steps (\code{hclust()} followed by
\code{\link[ape:as.phylo]{ape::as.phylo()}} followed by \code{\link[ggtree:ggtree]{ggtree::ggtree()}}) match
\code{\link[=motif_tree]{motif_tree()}} exactly, so the visual conventions, the \code{linecol} /
\code{labels} / \code{tipsize} slot-mapping arguments, and the \code{dist}-input
short-circuit all behave the same.
}
\details{
The PCC-to-distance conversion \code{d = (1 - score) / 2} is symmetric
(the comparison matrix from \code{\link[=compare_motifs_lite]{compare_motifs_lite()}} is symmetric in
matrix mode), so \code{as.dist()} and \code{hclust()} accept it directly.
Anti-correlated motif pairs land at the maximum distance of \code{1};
identical motifs land at \code{0}.

For more control over tree construction, run \code{\link[=compare_motifs_lite]{compare_motifs_lite()}}
directly with \code{matrix.out = "score"}, convert to a \code{dist} object
yourself, and pass that \code{dist} to \code{motif_tree_lite()}. The function
detects the \code{dist} input and skips the comparison step.
}
\examples{
\dontrun{
library(universalmotif)
jaspar <- read_jaspar(system.file("extdata", "jaspar.txt",
                                  package = "universalmotif"))
if (requireNamespace("ggtree", quietly = TRUE)) {
  motif_tree_lite(jaspar, linecol = "none", labels = "name",
              layout = "rectangular")
}

## Equivalent two-step form, useful when you want to inspect the
## distance matrix before drawing the tree:
if (requireNamespace("ggtree", quietly = TRUE)) {
  score.mat <- compare_motifs_lite(jaspar, matrix.out = "score")
  d <- as.dist((1 - score.mat) / 2)
  motif_tree_lite(d, labels = "name")
}
}

}
\seealso{
\code{\link[=motif_tree]{motif_tree()}}, \code{\link[=compare_motifs_lite]{compare_motifs_lite()}}, \code{\link[=merge_similar_lite]{merge_similar_lite()}},
\code{\link[=geom_logo]{geom_logo()}}, \code{\link[=view_motifs_lite]{view_motifs_lite()}}, \code{\link[ggtree:ggtree]{ggtree::ggtree()}}

Other lite motif functions: 
\code{\link{compare_motifs_lite}()},
\code{\link{enrich_motifs_lite}()},
\code{\link{merge_motifs_lite}()},
\code{\link{merge_similar_lite}()},
\code{\link{scan_sequences_lite}()},
\code{\link{view_motifs_lite}()}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
\concept{lite motif functions}
