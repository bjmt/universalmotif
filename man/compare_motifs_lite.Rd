% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_motifs_lite.R
\name{compare_motifs_lite}
\alias{compare_motifs_lite}
\title{Faster minimalist motif comparison.}
\usage{
compare_motifs_lite(motifs, compare.to = NULL, qvalue = 0.1,
  min.overlap = 5L, RC = TRUE, null = c("empirical", "parametric"),
  bkg = NULL, matrix.out = c("score", "pvalue", "qvalue"), nthreads = 1,
  output.report, output.report.max.print = 10)
}
\arguments{
\item{motifs}{See \code{\link[=convert_motifs]{convert_motifs()}} for accepted motif formats. DNA or
RNA only.}

\item{compare.to}{\code{integer}, \code{character}, or \code{NULL}. When \code{NULL}
(default), all-pairs mode: every motif is compared against every
other and a square matrix is returned (see \code{matrix.out}). When
\code{integer}/\code{character}, the named/indexed motifs are treated as
queries and the \emph{entire} input is treated as the target database;
a long-format \code{data.frame} of significant matches is returned.}

\item{qvalue}{\code{numeric(1)}. q-value cutoff for the long-format output.
Default \code{0.1}, matching \verb{yamtk cmp}. Ignored in matrix mode.}

\item{min.overlap}{\code{integer(1)}. Minimum overlap length (in columns)
for a candidate alignment. Default \code{5}.}

\item{RC}{\code{logical(1)}. If \code{TRUE} (default), also score query motifs
in reverse-complement orientation and report the best of the two.}

\item{null}{\code{character(1)}. Null-PMF mode: \code{"empirical"} (default) or
\code{"parametric"}. See Details.}

\item{bkg}{\code{numeric(4)} or \code{NULL}. Background base frequencies (A, C,
G, T). If \code{NULL}, uniform \code{c(.25, .25, .25, .25)} is used. Critical
for \code{"parametric"} (sets the Dirichlet prior); affects only
pseudocount smoothing for \code{"empirical"}.}

\item{matrix.out}{\code{character(1)}. In matrix mode, which metric to
return: \code{"score"} (mean PCC; default), \code{"pvalue"}, or \code{"qvalue"}.
Ignored in long-format mode.}

\item{nthreads}{\code{numeric(1)}. Number of threads. \code{nthreads = 0} uses
all available threads.}

\item{output.report}{\code{character(1)}. Path (ending in \code{.html}) for a
self-contained HTML report of the matches: a summary header, a sortable
results table (query, target, offset, strand, overlap, score, P/q-value),
and a query-vs-target sequence-logo alignment per top match. Requires
\code{compare.to} to be set and the \code{knitr} and \code{rmarkdown} packages (plus
pandoc). Only meaningful in long-format mode.}

\item{output.report.max.print}{\code{numeric(1)}. Maximum number of top matches
to show in the report (table and figures). Default \code{10}.}
}
\value{
\itemize{
\item \strong{Matrix mode} (\code{compare.to = NULL}): a square numeric matrix
indexed by motif name, holding the value selected by \code{matrix.out}.
\item \strong{Long-format mode} (\code{compare.to} provided): a \code{data.frame} with
columns
\code{subject}, \code{subject.i}, \code{target}, \code{target.i}, \code{offset}, \code{strand},
\code{overlap}, \code{score}, \code{subject.consensus}, \code{target.consensus},
\code{pvalue}, \code{qvalue}, filtered to \code{qvalue <= qvalue} and sorted by
q-value ascending. Coordinates are 1-based; \code{offset} is the query
column where target column 1 aligns.
}
}
\description{
\code{compare_motifs_lite()} is a deliberately pared-down counterpart to
\code{\link[=compare_motifs]{compare_motifs()}}, with defaults and an algorithm that mirror those of
the command-line tool \href{https://github.com/bjmt/yamtk}{yamtk}. It uses the
per-column Pearson correlation as its similarity metric, computes
significance from a TOMTOM-style null PMF (either empirical, from the
target database, or parametric, via a Dirichlet-Multinomial over a K=5
simplex grid), applies both Bonferroni and Benjamini-Hochberg adjustment,
and returns either a long-format \code{data.frame} of the significant pairs or
a square score / p-value / q-value matrix.
}
\details{
Use \code{\link[=compare_motifs]{compare_motifs()}} when you need any of the broader features:
alternative similarity metrics (EUCL, ALLR, KL, …), score-aggregation
strategies, IC-based filters, multifreq scoring, ICM/PPM dispatch,
relative-entropy mode, or the JASPAR-derived \code{db.scores} lookup-table
p-value path.

P-values are computed in two modes mirroring \verb{yamtk cmp}:

\describe{
\item{\code{null = "empirical"} (default)}{Per-query column, score
against every target-database column, histogram into 51 PCC bins,
convolve to overlap length L. Cost per query is O(N x T) where N
is the number of target motifs and T their mean width. Best for
databases with more than a few hundred total columns.}
\item{\code{null = "parametric"}}{Enumerate the 56 compositions of A/C/G/T
on a K=5 simplex grid, weight each by a Dirichlet-Multinomial PMF
under alpha = 4 * bkg, score against the query column. Per-query
cost is constant in database size. Best for small databases or
when histogram sparsity would distort the empirical estimate.}
}

The reported \code{score} is the mean PCC over the overlap (always in
\eqn{[-1, 1]}), not the raw sum.
}
\examples{
library(universalmotif)
set.seed(1)
motifs <- lapply(1:6, function(i)
  create_motif(paste(sample(c("A","C","G","T"), 8, replace = TRUE),
                     collapse = ""), name = paste0("M", i)))
## Matrix of mean PCC scores
m <- compare_motifs_lite(motifs)
round(m, 2)
## Long-format hits at q <= 0.5 with motif 1 as the query
compare_motifs_lite(motifs, compare.to = 1, qvalue = 0.5)

}
\references{
Gupta S, Stamatoyannopoulos JA, Bailey TL, Noble WS (2007). "Quantifying
similarity between motifs." \emph{Genome Biology}, \strong{8}(2), R24.

Tremblay BJM (2026). yamtk: Yet Another Motif ToolKit.
\url{https://github.com/bjmt/yamtk}.
}
\seealso{
\code{\link[=compare_motifs]{compare_motifs()}}, \code{\link[=scan_sequences_lite]{scan_sequences_lite()}}, \code{\link[=view_motifs_lite]{view_motifs_lite()}}

Other lite motif functions: 
\code{\link{enrich_motifs_lite}()},
\code{\link{merge_motifs_lite}()},
\code{\link{merge_similar_lite}()},
\code{\link{motif_tree_lite}()},
\code{\link{scan_sequences_lite}()},
\code{\link{view_motifs_lite}()}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
\concept{lite motif functions}
