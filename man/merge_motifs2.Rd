% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_motifs2.R
\name{merge_motifs2}
\alias{merge_motifs2}
\title{Merge a list of motifs into a single consensus motif (v2).}
\usage{
merge_motifs2(motifs, min.overlap = 5L, RC = TRUE, new.name = NULL,
  weighted = FALSE, nthreads = 1)
}
\arguments{
\item{motifs}{\code{list} of motifs. See \code{\link[=convert_motifs]{convert_motifs()}} for accepted
formats. DNA or RNA only.}

\item{min.overlap}{\code{integer(1)}. Minimum overlap (in columns) for an
alignment to be considered. Default \code{5L}. Forwarded to
\code{\link[=compare_motifs2]{compare_motifs2()}}.}

\item{RC}{\code{logical(1)}. If \code{TRUE} (default), also test reverse-complement
alignments. Motifs whose best alignment hits the \code{-} strand of the
anchor are reverse-complemented before averaging, so the merged
motif is always returned in the anchor's orientation.}

\item{new.name}{\code{character(1)} or \code{NULL}. Name for the merged motif.
If \code{NULL} (default), set to \code{paste0("merged_", paste(names, collapse = "+"))}.}

\item{weighted}{\code{logical(1)}. If \code{TRUE}, weight each input motif's
column contribution by its \verb{@nsites} slot. Default \code{FALSE} (simple
unweighted mean) -- \verb{@nsites} is often missing, set to a sentinel
like 100, or inconsistent across motifs from different sources, so
weighting can do more harm than good unless the user has actively
curated nsites. Motifs whose \verb{@nsites} is missing or zero fall back
to weight 1 in either mode.}

\item{nthreads}{\code{numeric(1)}. Number of threads passed to
\code{\link[=compare_motifs2]{compare_motifs2()}}. \code{nthreads = 0} uses all available threads.}
}
\value{
A single \code{universalmotif} S4 object of type \code{"PPM"}.
}
\description{
\code{merge_motifs2()} is a minimalist counterpart to \code{\link[=merge_motifs]{merge_motifs()}} that
aligns motifs using \code{compare_motifs2()}'s alignment finder and averages
their position-probability columns in a single shared coordinate frame.
All alignment / similarity decisions go through \code{compare_motifs2()}
(Pearson correlation, optionally reverse-complement aware), so the
merge behaviour stays in step with the rest of the v2 stack.
}
\details{
Unlike \code{\link[=merge_motifs]{merge_motifs()}}, the algorithm is \strong{anchor-based}, not
iterative-pairwise: the highest-IC input motif is chosen as the
anchor, every other motif is aligned against it once, and all motifs
are then averaged together in a single shared column frame. This
removes the input-order dependence of the v1 iterative scheme.

For each non-anchor motif, \code{\link[=compare_motifs2]{compare_motifs2()}} gives the best
alignment offset, strand, and overlap against the anchor. Each
aligned motif (reverse-complemented if its best alignment is \code{-}
strand) is then placed in a unified column coordinate frame whose
origin coincides with the anchor's first column. The output's column
at frame position \code{p} is the (optionally \verb{@nsites}-weighted) mean of
the PPM columns from all input motifs that cover \code{p}; positions
covered by only some motifs are still emitted (no automatic
IC-trimming). Run \code{\link[=trim_motifs]{trim_motifs()}} on the result if you want to
shrink low-information flanks.

The merged motif's \verb{@bkg} is the unweighted mean of the input
motifs' \verb{@bkg} vectors (or uniform if missing). \verb{@nsites} is the
sum of input \verb{@nsites}.
}
\examples{
library(universalmotif)
m1 <- create_motif("TTGACATA", name = "a")
m2 <- create_motif("CTTGACAT", name = "b")
m3 <- create_motif("TGACATAT", name = "c")
merged <- merge_motifs2(list(m1, m2, m3))
merged

}
\seealso{
\code{\link[=merge_motifs]{merge_motifs()}}, \code{\link[=merge_similar2]{merge_similar2()}}, \code{\link[=compare_motifs2]{compare_motifs2()}},
\code{\link[=trim_motifs]{trim_motifs()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
