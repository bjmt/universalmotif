% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motif_proximity.R
\name{plot_motif_proximity}
\alias{plot_motif_proximity}
\title{Plot the distance-to-anchor distribution of motif hits.}
\usage{
plot_motif_proximity(proximity, motifs = NULL, ncol = 2L, bins = 50L,
  fill.zone = "#ffe4a3", log.x = FALSE)
}
\arguments{
\item{proximity}{\code{data.frame} returned by \code{\link[=motif_proximity]{motif_proximity()}}. Must
carry the \code{dist.to.nearest} list-column.}

\item{motifs}{\code{character} or \code{NULL}. Subset to these motifs (by name).
\code{NULL} (default) plots every row of \code{proximity}.}

\item{ncol}{\code{integer(1)}. Number of facet columns. Default \code{2}.}

\item{bins}{\code{integer(1)}. Histogram bin count. Default \code{50}.}

\item{fill.zone}{\code{character(1)}. Colour of the best-zone shade.
Default \code{"#ffe4a3"} (a pale yellow).}

\item{log.x}{\code{logical(1)}. Plot \code{log10} of the absolute distance
instead of the signed distance. Default \code{FALSE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Companion to \code{\link[=motif_proximity]{motif_proximity()}}. Returns a \code{ggplot} faceted by motif,
showing a histogram of per-hit distances to the nearest anchor (signed
when the anchors are stranded, so upstream and downstream sit on
opposite sides of zero) with the most-enriched near zone shaded.
}
\seealso{
\code{\link[=motif_proximity]{motif_proximity()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
