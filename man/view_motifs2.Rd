% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view_motifs2.R
\name{view_motifs2}
\alias{view_motifs2}
\title{Plot motif logos using \code{compare_motifs2()} alignment (v2).}
\usage{
view_motifs2(motifs, use.type = "ICM", tryRC = TRUE, min.overlap = 6,
  relative_entropy = FALSE, nthreads = 1, return.raw = FALSE,
  dedup.names = TRUE, show.positions = TRUE, show.positions.once = TRUE,
  sort.by = c("none", "ic", "similarity"), show.names = TRUE,
  names.pos = c("top", "right"), use.freq = 1, colour.scheme = NULL,
  fontDF = NULL, min.height = 0.01, x.spacer = if (use.freq == 1) 0.04
  else 0.1, y.spacer = 0.01, sort.positions = !use.type \%in\% c("PCM",
  "PPM"), sort.positions.decreasing = TRUE, text.size = 16,
  fit.to.height = if (use.type == "PPM") 1 else NULL, RC.text = " [RC]",
  flip.neg = FALSE)
}
\arguments{
\item{motifs}{See \code{\link[=convert_motifs]{convert_motifs()}} for accepted motif formats.
DNA or RNA only.}

\item{use.type}{\code{character(1)} One of \code{c('PCM', 'PPM', 'PWM', 'ICM')}.
Controls the matrix type used for display.}

\item{tryRC}{\code{logical(1)} Also test reverse-complement alignments.
Default \code{TRUE}.}

\item{min.overlap}{\code{integer(1)} Minimum overlap (in columns) for an
alignment to be accepted. Default \code{6}.}

\item{relative_entropy}{\code{logical(1)} If \code{TRUE}, compute information
content using the motif's background frequencies (relative-entropy
ICM). Used only for display when \code{use.type = "ICM"}.}

\item{nthreads}{\code{numeric(1)} Number of threads for comparison.
\code{nthreads = 0} uses all available threads.}

\item{return.raw}{\code{logical(1)} Instead of returning a plot, return
the aligned named matrices used to generate the plot.}

\item{dedup.names}{\code{logical(1)} Allow duplicate motif names to be
silently disambiguated for plotting.}

\item{show.positions}{\code{logical(1)} Show x-axis position tick labels.}

\item{show.positions.once}{\code{logical(1)} When plotting multiple
motifs, show x-axis position tick labels only once.}

\item{sort.by}{\code{character(1)}. Display order for multi-motif plots.
One of \code{"none"} (default), \code{"ic"}, or \code{"similarity"}. Alignment
is always performed with the highest-IC motif as the anchor;
\code{sort.by} controls only the order in which the aligned motifs
are shown.}

\item{show.names}{\code{logical(1)} Add motif names when plotting
multiple motifs.}

\item{names.pos}{\code{character(1)} Motif name locations, either
\code{"top"} or \code{"right"}.}

\item{use.freq}{\code{numeric(1)} Plot higher-order motifs from the
\code{multifreq} slot.}

\item{colour.scheme}{\code{character} Named character vector of colour
names. Default colours are provided for DNA, RNA, and AA motifs
if \code{NULL}.}

\item{fontDF}{\code{data.frame} or \code{DataFrame} Polygon data for letters.}

\item{min.height}{\code{numeric(1)} Minimum letter height to show, as a
fraction of total plot height.}

\item{x.spacer}{\code{numeric(1)} Horizontal spacing between letters.}

\item{y.spacer}{\code{numeric(1)} Vertical spacing between letters.}

\item{sort.positions}{\code{logical(1)} Sort letters vertically per
position by height.}

\item{sort.positions.decreasing}{\code{logical(1)} Sort direction.}

\item{text.size}{\code{numeric(1)} Text size for labels.}

\item{fit.to.height}{\code{numeric(1)} Normalise per-position height.}

\item{RC.text}{\code{character(1)} Text appended to motif names shown
as their reverse complement.}

\item{flip.neg}{\code{logical(1)} Flip letters with negative heights.}
}
\value{
A \code{ggplot} object. If \code{return.raw = TRUE}, a list of
matrices.
}
\description{
\code{view_motifs2()} is the leaner counterpart of \code{\link[=view_motifs]{view_motifs()}}: it aligns
the input motifs using the same Pearson-correlation backend used by
\code{\link[=compare_motifs2]{compare_motifs2()}} and \code{\link[=merge_motifs2]{merge_motifs2()}}.
The rendering (logo polygons, \code{facet_wrap()} layout, \code{sort.by} permutation,
\code{names.pos} placement) is identical to \code{\link[=view_motifs]{view_motifs()}}.
}
\details{
DNA and RNA only. For amino-acid or custom-alphabet motifs use
\code{\link[=view_motifs]{view_motifs()}}.

The alignment step uses the same
backend as \code{\link[=merge_motifs2]{merge_motifs2()}} and \code{\link[=merge_similar2]{merge_similar2()}}. The
highest-information-content input motif is chosen as the anchor,
every other motif is aligned against it once, motifs whose best
alignment lands on the reverse complement strand are
reverse-complemented before display, and all motifs are placed in
a shared column frame with zero-padding on either side as needed.
The padded matrices then flow through the same logo-rendering
pipeline used by \code{\link[=view_motifs]{view_motifs()}}.

See \code{\link[=view_motifs]{view_motifs()}} for the original version, which supports
Euclidean / similarity metrics and arbitrary alphabets.
}
\examples{
m1 <- create_motif("TTGACATA", name = "a")
m2 <- create_motif("CTTGACAT", name = "b")
m3 <- create_motif("TGACATAT", name = "c")
view_motifs2(list(m1, m2, m3))
view_motifs2(list(m1, m2, m3), sort.by = "similarity",
             names.pos = "right")

}
\seealso{
\code{\link[=view_motifs]{view_motifs()}}, \code{\link[=view_logo]{view_logo()}}, \code{\link[=compare_motifs2]{compare_motifs2()}},
\code{\link[=merge_motifs2]{merge_motifs2()}}, \code{\link[=merge_similar2]{merge_similar2()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
