% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motif_coocc.R
\name{plot_motif_coocc}
\alias{plot_motif_coocc}
\title{Heatmap of motif-pair co-occurrence q-values or odds ratios.}
\usage{
plot_motif_coocc(coocc, fill = c("neglog10q", "odds_ratio"), q.cap = 50,
  cluster = TRUE)
}
\arguments{
\item{coocc}{\code{data.frame}. Output of \code{\link[=motif_coocc]{motif_coocc()}}.}

\item{fill}{One of \code{"neglog10q"} (default) or \code{"odds_ratio"}. Which
column to use for the tile fill.}

\item{q.cap}{\code{numeric(1)}. Cap on \code{-log10(qvalue)} to keep one
extreme pair from dominating the colour scale. Default \code{50}.}

\item{cluster}{\code{logical(1)}. If \code{TRUE} (default) and the motif set
contains > 2 motifs, both axes are ordered by hierarchical
clustering on \code{(max - value)} distances. Falls back to input
order otherwise.}
}
\value{
A \code{ggplot} object.
}
\description{
Companion plot helper for \code{\link[=motif_coocc]{motif_coocc()}}. Builds a square
motif x motif tile heatmap from the long-format pair table,
optionally clustering motifs to bring similar pairs together.
}
\examples{
\dontrun{
co <- motif_coocc(motifs, seqs)
plot_motif_coocc(co)
plot_motif_coocc(co, fill = "odds_ratio", cluster = FALSE)
}

}
\seealso{
\code{\link[=motif_coocc]{motif_coocc()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
