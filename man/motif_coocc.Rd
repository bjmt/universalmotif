% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motif_coocc.R
\name{motif_coocc}
\alias{motif_coocc}
\title{Find significantly co-occurring motif pairs in a set of sequences.}
\usage{
motif_coocc(motifs, sequences = NULL, hits = NULL, n.sequences = NULL,
  pvalue = 1e-04, RC = TRUE, no.overlaps = FALSE,
  no.overlaps.by = c("pvalue", "score"), max.distance = NULL,
  min.coocc = 1L, pseudocount = 0L, self.pairs = FALSE, nthreads = 1L)
}
\arguments{
\item{motifs}{See \code{\link[=convert_motifs]{convert_motifs()}} for accepted motif formats.
A single motif or a list of motifs. Required in both paths to
provide motif names and to dimension the pair table.}

\item{sequences}{\code{XStringSet} or \code{NULL}. If supplied, the function
scans internally via \code{\link[=scan_sequences_lite]{scan_sequences_lite()}}; DNA/RNA only.}

\item{hits}{\code{data.frame}, \code{GRanges}, or \code{NULL}. Precomputed hit
table with \code{motif.i} and \code{sequence.i} columns. If supplied,
\code{sequences} is ignored and \code{n.sequences} must be provided. Any
alphabet accepted on this path.}

\item{n.sequences}{\code{integer(1)} or \code{NULL}. Total number of host
sequences (needed to fill the "neither" cell of the 2x2 when
using the precomputed-hits path).}

\item{pvalue}{\code{numeric(1)}. Per-hit p-value threshold for the
internal scan. Default \code{1e-4}. Ignored on the hit-table path.}

\item{RC}{\code{logical(1)}. Scan the reverse complement too? Default
\code{TRUE}. Ignored when a hits table is provided.}

\item{no.overlaps}{\code{logical(1)}. Passed to \code{\link[=scan_sequences_lite]{scan_sequences_lite()}} on the
internal-scan path: if \code{TRUE}, overlapping hits of the \emph{same} motif
are collapsed to one (greedy; see \code{\link[=dedup_hits]{dedup_hits()}}) before
co-occurrence is computed. This does not change the presence-based
Fisher test (a sequence either contains a motif or not, however many
overlapping copies it has), so it only affects the descriptive
spatial columns reported under \code{max.distance}, where it stops
tandem/overlapping matches of one motif being counted as separate
instances. Overlaps \emph{between} the two motifs of a pair are always
kept, so heterodimer footprints are preserved. Ignored (with a
warning) when a precomputed \code{hits} table is supplied. Default
\code{FALSE}.}

\item{no.overlaps.by}{\code{character(1)}. Tie-break priority when
\code{no.overlaps = TRUE}: \code{"pvalue"} (default) keeps the lowest-p hit,
\code{"score"} the highest-scoring. Passed to \code{\link[=scan_sequences_lite]{scan_sequences_lite()}}.}

\item{max.distance}{\code{integer(1)} or \code{NULL}. If non-\code{NULL}, two
extra descriptive columns are added to the output:
\code{both.clustered} (subset of co-occurring sequences with a within-
\code{max.distance} (A, B) hit pair) and \code{median.distance} (median of
those nearest-pair distances). The Fisher p-value is unchanged;
it always tests "do A and B occur in the same sequences more
often than chance?" on the unfiltered 2x2. Requires a \code{start}
column on the hit table.}

\item{min.coocc}{\code{integer(1)}. Pairs with fewer than \code{min.coocc}
co-occurring sequences are tagged with NA p- and q-values
(not tested). Default \code{1L} (only test pairs that co-occur at
least once).}

\item{pseudocount}{\code{numeric(1)}. Added to every cell of the 2x2
contingency before the Fisher test (matches the convention from
\code{\link[=enrich_motifs_lite]{enrich_motifs_lite()}}). Default \code{0L}.}

\item{self.pairs}{\code{logical(1)}. If \code{TRUE}, include \verb{(i, i)} rows
(testing whether motif \code{i} co-occurs with itself). Usually only
meaningful for motifs that bind as homodimers. Default \code{FALSE}.}

\item{nthreads}{\code{integer(1)}. Threads for the internal scan (when
\code{sequences} is supplied). The pairwise Fisher loop is single-
threaded R code regardless.}
}
\value{
\code{data.frame} with columns:
\itemize{
\item \code{motif_a}, \code{motif_b}: motif names (1-based indexing into the
supplied \code{motifs} list; deduplicated names if duplicates exist).
\item \code{a_only}, \code{b_only}, \code{both}, \code{neither}: 2x2 contingency cells.
\item \code{odds_ratio}: conditional MLE odds ratio from \code{fisher.test}.
\item \code{pvalue}: one-sided Fisher's exact p-value
(alternative = "greater"). NA when \code{both < min.coocc}.
\item \code{qvalue}: BH-corrected q-value across all tested pairs.
\item \code{both.clustered} (spatial mode only): subset of \code{both}
where a within-\code{max.distance} (A, B) hit pair exists.
\item \code{median.distance} (spatial mode only): median of those
nearest-pair distances; \code{NA} if no clustered pair.
Rows are sorted by q-value ascending; NA-pvalue rows go last.
}
}
\description{
\code{\link[=motif_coocc]{motif_coocc()}} tests every motif pair for over-co-occurrence across
a set of sequences using a one-sided Fisher's exact test on the 2x2
contingency table of per-sequence presence/absence. The result is
the long-format pair table, BH-corrected across all tested pairs.
Optionally, when \code{max.distance} is non-\code{NULL}, the function also
reports two descriptive spatial columns, \code{both.clustered}
(number of co-occurring sequences with a within-\code{max.distance}
(A, B) hit pair) and \code{median.distance} (the median nearest-pair
spacing), so users can flag heterodimer-like arrangements. The
Fisher p-value itself is always computed on the unfiltered 2x2
(the spatial filter is informational, not a test).
}
\details{
Two input paths are supported:
\enumerate{
\item Internal scan (default): pass \code{motifs} + \code{sequences} and
\code{motif_coocc()} will call \code{\link[=scan_sequences_lite]{scan_sequences_lite()}} with the given
\code{pvalue} and \code{RC} arguments to build the hit table. DNA/RNA
only (the restriction comes from \code{scan_sequences_lite()}).
\item Precomputed hits: pass \code{motifs} + \code{hits} + \code{n.sequences}.
\code{hits} is a \code{data.frame} or \code{GRanges} with \code{motif.i} and
\code{sequence.i} columns (the native return shape of
\code{\link[=scan_sequences]{scan_sequences()}} / \code{\link[=scan_sequences_lite]{scan_sequences_lite()}}), optionally \code{start}
when \code{max.distance} is set. This path accepts any motif
alphabet; once a hit table exists, co-occurrence is pure
set arithmetic on integer indices and no sequence bytes are
read.
}
}
\examples{
\dontrun{
library(universalmotif)
data(ArabidopsisMotif)
seqs <- create_sequences("DNA", seqnum = 200, seqlen = 500)
## Toy: implant two copies of the same motif jointly in all seqs.
set.seed(1)
imp <- implant_motifs(list(ArabidopsisMotif, ArabidopsisMotif),
                      seqs, n.per.seq = 1, return.indices = TRUE)
co <- motif_coocc(list(ArabidopsisMotif, ArabidopsisMotif),
                  imp$sequences, pvalue = 1e-3)
co
}

}
\references{
Heinz S, Benner C, Spann N, et al. (2010). "Simple combinations of
lineage-determining transcription factors prime cis-regulatory
elements required for macrophage and B cell identities."
\emph{Molecular Cell}, \strong{38}(4):576-589. \doi{10.1016/j.molcel.2010.05.004}.
(HOMER's pairwise-Fisher co-occurrence convention.)

Whitington T, Frith MC, Johnson J, Bailey TL (2011). "Inferring
transcription factor complexes from ChIP-seq data." \emph{Nucleic Acids
Research}, \strong{39}(15):e98. \doi{10.1093/nar/gkr341}. (SpaMo, the
spatial-distance motif-pair analysis.)
}
\seealso{
\code{\link[=scan_sequences_lite]{scan_sequences_lite()}}, \code{\link[=enrich_motifs_lite]{enrich_motifs_lite()}}, \code{\link[=motif_peaks]{motif_peaks()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
