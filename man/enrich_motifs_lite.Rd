% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_motifs_lite.R
\name{enrich_motifs_lite}
\alias{enrich_motifs_lite}
\title{Fast minimalist motif enrichment.}
\usage{
enrich_motifs_lite(motifs, sequences, bkg.sequences = NULL, pvalue = 1e-04,
  qvalue = 0.1, test = c("seqs", "sites"), RC = TRUE, shuffle.k = 2L,
  rng.seed = sample.int(1e+06, 1), pseudocount = 1L, nthreads = 1)
}
\arguments{
\item{motifs}{See \code{\link[=convert_motifs]{convert_motifs()}} for accepted motif formats. DNA or
RNA only; amino-acid and custom alphabet motifs are rejected.}

\item{sequences}{\code{XStringSet}. Target sequences.}

\item{bkg.sequences}{\code{XStringSet} or \code{NULL}. Background sequences. If
\code{NULL} (default), target sequences are shuffled k-let-conserving via
\code{\link[=shuffle_sequences]{shuffle_sequences()}} with \code{k = shuffle.k}, matching both
\code{enrich_motifs()} and \verb{yamtk enr} default behaviour.}

\item{pvalue}{\code{numeric(1)}. P-value cutoff passed to \code{\link[=scan_sequences_lite]{scan_sequences_lite()}}
for reporting individual hits. Default \code{1e-4}.}

\item{qvalue}{\code{numeric(1)}. Result-level q-value cutoff. Motifs whose
BH-adjusted enrichment p-value exceeds this are filtered out. Default
\code{0.1}.}

\item{test}{\code{character(1)}. One of \code{"seqs"} (default) or \code{"sites"}.
\code{"seqs"} runs Fisher's exact on per-sequence hit presence (does this
sequence contain >=1 hit?), equivalent to \code{\link[=enrich_motifs]{enrich_motifs()}}'s
\code{mode = "seq.hits"}. \code{"sites"} runs Fisher's exact on per-position
rates (how many hits per scannable position?), equivalent to
\code{mode = "total.hits"}.}

\item{RC}{\code{logical(1)}. If \code{TRUE} (default), scan both strands.}

\item{shuffle.k}{\code{integer(1)}. K-let size for the background shuffle
(only used when \code{bkg.sequences = NULL}). Default \code{2L}.}

\item{rng.seed}{\code{integer(1)}. RNG seed for background shuffling.
Default \code{sample.int(1e6, 1)}; set explicitly for reproducible runs.}

\item{pseudocount}{\code{integer(1)}. Pseudocount added uniformly to each
cell of the Fisher 2x2 table; useful when one side has zero counts.
Default \code{1L} (matches yamtk's \verb{-p 1} default).}

\item{nthreads}{\code{numeric(1)}. Number of threads passed to
\code{\link[=scan_sequences_lite]{scan_sequences_lite()}} and \code{\link[=shuffle_sequences]{shuffle_sequences()}}. \code{nthreads = 0} uses
all available threads.}
}
\value{
A \code{data.frame} with one row per significant motif, sorted by
\code{pvalue} ascending. Columns:

\itemize{
\item \code{motif}: motif name
\item \code{motif.i}: 1-based index into the input \code{motifs}
\item \code{consensus}: IUPAC consensus of the motif
\item \code{target.seq.n}: number of target sequences scanned
\item \code{target.seq.hits}: number of target sequences containing >=1 hit
\item \code{target.site.hits}: total hit count across all target sequences
\item \code{bkg.seq.n}: number of background sequences scanned
\item \code{bkg.seq.hits}: number of background sequences containing >=1 hit
\item \code{bkg.site.hits}: total hit count across all background sequences
\item \code{enrichment}: fold-enrichment (rate ratio; floor of 0.5/n in the
denominator to avoid division by zero), per yamtk's formula
\item \code{log2.enrichment}: \code{log2(enrichment)}
\item \code{pvalue}: Fisher's exact one-sided ("greater") p-value
\item \code{qvalue}: Benjamini-Hochberg adjusted p-value across motifs
}
}
\description{
\code{enrich_motifs_lite()} is a deliberately pared-down counterpart to
\code{\link[=enrich_motifs]{enrich_motifs()}}, with a default surface that mirrors the command-line
tool \href{https://github.com/bjmt/yamtk}{yamtk}. It exposes a single p-value
cutoff for the hits, a single q-value cutoff for the results, and two
test modes (\code{"seqs"} and \code{"sites"}, both Fisher's exact), and it leans on
\code{\link[=scan_sequences_lite]{scan_sequences_lite()}} under the hood to scan the target and background
sequences. The p-value adjustment is hard-coded to Benjamini-Hochberg.
}
\details{
Use \code{\link[=enrich_motifs]{enrich_motifs()}} when you need any of: q-value adjustment methods
other than BH; multifreq / gapped motifs; multiple-testing E-values;
the \code{respect.strand}, \code{allow.nonfinite}, or non-pvalue \code{threshold.type}
machinery; or amino-acid motifs.

Internally, \code{\link[=scan_sequences_lite]{scan_sequences_lite()}} is run once on the target set and once on
the background set; the resulting hit tables are aggregated per motif into
a 2x2 contingency table whose entries depend on \code{test}:

\describe{
\item{\code{"seqs"}}{
\code{a} = target sequences with >=1 hit;
\code{b} = background sequences with >=1 hit;
\code{c} = target sequences with no hits;
\code{d} = background sequences with no hits.
}
\item{\code{"sites"}}{
\code{a} = total hits in target;
\code{b} = total hits in background;
\code{c} = scannable target positions minus \code{a};
\code{d} = scannable background positions minus \code{b}.
Scannable positions are computed per motif as
\code{sum(pmax(width(seqs) - motif_width + 1, 0))}, doubled if \code{RC = TRUE}.
}
}

Each cell has \code{pseudocount} added before \code{\link[stats:fisher.test]{stats::fisher.test()}} is run
with \code{alternative = "greater"}. Q-values are computed via
\code{p.adjust(method = "BH")} across all input motifs (not just the ones
that pass the filter), then rows with \code{qvalue > qvalue} are dropped.
}
\examples{
library(universalmotif)
data(ArabidopsisPromoters)
data(ArabidopsisMotif)
if (R.Version()$arch != "i386") {
  enrich_motifs_lite(ArabidopsisMotif, ArabidopsisPromoters,
                 pvalue = 1e-3, qvalue = 0.5, rng.seed = 1)
}

}
\references{
Tremblay BJM (2026). yamtk: Yet Another Motif ToolKit.
\url{https://github.com/bjmt/yamtk}.

McLeay R, Bailey TL (2010). "Motif Enrichment Analysis: A unified
framework and method evaluation." \emph{BMC Bioinformatics}, \strong{11}.
}
\seealso{
\code{\link[=enrich_motifs]{enrich_motifs()}}, \code{\link[=scan_sequences_lite]{scan_sequences_lite()}}, \code{\link[=shuffle_sequences]{shuffle_sequences()}},
\code{\link[=match_bkg]{match_bkg()}}, \code{\link[=motif_pvalue]{motif_pvalue()}}

Other lite motif functions: 
\code{\link{compare_motifs_lite}()},
\code{\link{merge_motifs_lite}()},
\code{\link{merge_similar_lite}()},
\code{\link{motif_tree_lite}()},
\code{\link{scan_sequences_lite}()},
\code{\link{view_motifs_lite}()}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
\concept{lite motif functions}
