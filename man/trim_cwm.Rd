% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trim_cwm.R
\name{trim_cwm}
\alias{trim_cwm}
\title{Trim CWM motifs by absolute column sum.}
\usage{
trim_cwm(motifs, trim.threshold = 0.3, abs.threshold = NULL,
  trim.from = c("both", "left", "right"))
}
\arguments{
\item{motifs}{A CWM motif or list of CWM motifs (\code{type = "CWM"}).}

\item{trim.threshold}{\code{numeric(1)}. Fraction-of-peak cutoff,
used when \code{abs.threshold} is \code{NULL}. Default \code{0.3}.}

\item{abs.threshold}{\code{numeric(1)} or \code{NULL}. Absolute-value
cutoff on \verb{sum_i |cwm[i,j]|}. When non-\code{NULL}, takes
precedence over \code{trim.threshold}. Default \code{NULL}.}

\item{trim.from}{\code{character(1)}. One of \code{"both"}, \code{"left"}, or
\code{"right"}. Direction of edge trimming.}
}
\value{
A trimmed CWM motif (single input) or list of trimmed
CWM motifs (list input). The \code{type = "CWM"} tag is preserved.
}
\description{
Trim the low-contribution edge columns of one or more
Contribution Weight Matrix (CWM) motifs. CWMs do not have a
probabilistic interpretation, so \code{\link[=trim_motifs]{trim_motifs()}}'s
information-content threshold is not the natural cutoff;
instead, \code{\link[=trim_cwm]{trim_cwm()}} drops edge columns whose absolute
column sum (\verb{sum_i |cwm[i,j]|}) falls below a threshold.
}
\details{
Two threshold modes are supported. By default, \code{trim_cwm()}
uses the TF-MoDISco-lite \emph{fraction-of-peak} rule: drop edge
columns whose absolute column sum is below
\verb{trim.threshold * max_j(sum_i |cwm[i,j]|)}, with
\code{trim.threshold = 0.3} matching TF-MoDISco-lite's
\code{--trim_threshold} default. When \code{abs.threshold} is supplied
(non-\code{NULL}), the function switches to an absolute-value cutoff:
edge columns with absolute column sum below \code{abs.threshold} are
dropped, ignoring \code{trim.threshold}.

In both modes the trimmer walks inward only from the chosen
edges (\code{trim.from}), matching \code{\link[=trim_motifs]{trim_motifs()}}'s edge-only
semantics; interior low-contribution columns are kept.
}
\examples{
library(universalmotif)

## Synthetic CWM with a strong centre and weak flanks.
m <- matrix(c(0.02, -0.01,  0.01,  0.00,
              0.90, -0.30, -0.30, -0.30,
             -0.20,  0.80, -0.20, -0.20,
             -0.30, -0.30, -0.30,  0.90,
              0.03,  0.00, -0.02,  0.01),
            nrow = 4, byrow = FALSE,
            dimnames = list(c("A","C","G","T"), NULL))
cwm <- create_motif(m, type = "CWM", name = "demo")

## Default: drop edge columns where |colsum| < 0.3 * peak.
trimmed <- trim_cwm(cwm)
ncol(trimmed["motif"])

## Explicit absolute-value cutoff instead.
trim_cwm(cwm, abs.threshold = 0.5)

## One-sided trim.
trim_cwm(cwm, trim.from = "left")

}
\seealso{
\code{\link[=trim_motifs]{trim_motifs()}}, \code{\link[=create_motif]{create_motif()}}, \code{\link[=convert_type]{convert_type()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
