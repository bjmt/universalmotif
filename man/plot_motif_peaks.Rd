% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motif_peaks.R
\name{plot_motif_peaks}
\alias{plot_motif_peaks}
\title{Plot the position distribution of motif hits.}
\usage{
plot_motif_peaks(peaks, motifs = NULL, ncol = 2L, bins = 50L,
  fill.window = "#ffe4a3")
}
\arguments{
\item{peaks}{\code{data.frame} returned by \code{\link[=motif_peaks]{motif_peaks()}}. Must carry
the \code{centers} list-column.}

\item{motifs}{\code{character} or \code{NULL}. Subset to these motifs (by
name). \code{NULL} (default) plots every row of \code{peaks}.}

\item{ncol}{\code{integer(1)}. Number of facet columns. Default \code{2}.}

\item{bins}{\code{integer(1)}. Histogram bin count. Default \code{50}.}

\item{fill.window}{\code{character(1)}. Colour of the best-window shade.
Default \code{"#ffe4a3"} (a pale yellow).}
}
\value{
A \code{ggplot} object.
}
\description{
Companion to \code{\link[=motif_peaks]{motif_peaks()}}. Returns a \code{ggplot} faceted by motif,
showing a histogram of best-hit centre positions and a shaded
rectangle marking the most-enriched window.
}
\examples{
library(Biostrings)
set.seed(1)
seqs <- create_sequences(seqnum = 200, seqlen = 500, rng.seed = 1)
planted <- DNAString("TTGACATA")
for (i in seq_len(150)) {
  pos <- 246L + sample.int(8, 1) - 1L
  subseq(seqs[[i]], start = pos, width = 8) <- planted
}
m <- create_motif("TTGACATA", name = "test")
hits <- scan_sequences(m, seqs, threshold = 1e-3, threshold.type = "pvalue")
peaks <- motif_peaks(as.data.frame(hits), seq.length = 500, qvalue = 1)
plot_motif_peaks(peaks)

}
\seealso{
\code{\link[=motif_peaks]{motif_peaks()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
