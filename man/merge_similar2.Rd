% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_similar2.R
\name{merge_similar2}
\alias{merge_similar2}
\title{Cluster similar motifs by significance and merge each cluster.}
\usage{
merge_similar2(motifs, qvalue = 0.01, null = c("empirical", "parametric"),
  min.overlap = 5L, RC = TRUE, bkg = NULL, weighted = FALSE,
  return.clusters = FALSE, nthreads = 1)
}
\arguments{
\item{motifs}{\code{list} of motifs. DNA or RNA only.}

\item{qvalue}{\code{numeric(1)}. BH-adjusted q-value cutoff for linking
two motifs in the significance graph. Default \code{0.01}.}

\item{null}{\code{character(1)}. \code{"empirical"} (default) or \code{"parametric"},
forwarded to \code{\link[=compare_motifs2]{compare_motifs2()}}.}

\item{min.overlap}{\code{integer(1)}. Forwarded to \code{\link[=compare_motifs2]{compare_motifs2()}} /
\code{\link[=merge_motifs2]{merge_motifs2()}}. Default \code{5L}.}

\item{RC}{\code{logical(1)}. If \code{TRUE} (default), test reverse-complement
alignments.}

\item{bkg}{\code{numeric(4)} or \code{NULL}. Background frequencies, forwarded
to \code{\link[=compare_motifs2]{compare_motifs2()}}. \code{NULL} uses uniform.}

\item{weighted}{\code{logical(1)}. Forwarded to \code{\link[=merge_motifs2]{merge_motifs2()}}. Default
\code{FALSE}.}

\item{return.clusters}{\code{logical(1)}. If \code{TRUE}, return a
\code{universalmotif_df} of cluster assignments instead of merged
motifs. Default \code{FALSE}.}

\item{nthreads}{\code{numeric(1)}. Threads forwarded to \code{\link[=compare_motifs2]{compare_motifs2()}}
and \code{\link[=merge_motifs2]{merge_motifs2()}}. \code{nthreads = 0} uses all available threads.}
}
\value{
If \code{return.clusters = FALSE} (default): a \code{list} of
\code{universalmotif} S4 objects with similar motifs collapsed (the
list is shorter than the input whenever any merging occurred).
Singletons pass through unchanged.

If \code{return.clusters = TRUE}: a \code{universalmotif_df} (as produced
by \code{\link[=to_df]{to_df()}}) describing the input motifs -- one row per input
motif, with the \code{motif} column holding the motif objects and
\code{name} their names -- augmented with two extra columns:
\code{motif.i} (1-based input index) and \code{cluster} (1-based cluster
id; motifs in the same cluster share an id).
}
\description{
\code{merge_similar2()} is a faster minimalist counterpart to \code{\link[=merge_similar]{merge_similar()}}
that builds a pairwise similarity-significance graph using
\code{\link[=compare_motifs2]{compare_motifs2()}}'s empirical or parametric null q-values, finds
connected components on that graph, and collapses each component
(cluster) into a single merged motif via \code{\link[=merge_motifs2]{merge_motifs2()}}.
}
\details{
Unlike \code{\link[=merge_similar]{merge_similar()}} (which builds a distance matrix from raw
similarity scores and applies hierarchical clustering at an absolute
score cutoff) this function clusters on statistical
significance: two motifs are linked if their pairwise q-value
meets the user's \code{qvalue} cutoff. This matches the STAMP /
TOMTOM-clustering semantics ("group all motifs that are
significantly similar"), is deterministic, has no linkage-method
choice to expose, and lets the user reason about the cutoff in
q-value units rather than abstract distance.

The significance graph is built from a symmetrised q-value matrix:
\code{Qsym[i,j] = pmin(Q[i,j], Q[j,i])}. \code{\link[=compare_motifs2]{compare_motifs2()}}'s per-query BH
adjustment makes its native q-value matrix non-symmetric; the min-
symmetrisation defines a pair as linked if either direction meets
the cutoff (more permissive of the two, which is appropriate when
the goal is to group similar motifs).

Clustering is by connected components on the resulting graph,
implemented with a small union-find. Each component (size >= 2)
becomes a single merged motif via \code{\link[=merge_motifs2]{merge_motifs2()}}; singletons
(size == 1) pass through unchanged.
}
\examples{
\dontrun{
library(universalmotif)
## A clear cluster of 3 related motifs + 1 unrelated motif
m1 <- create_motif("TTGACATA", name = "a")
m2 <- create_motif("CTTGACAT", name = "b")
m3 <- create_motif("TGACATAT", name = "c")
m4 <- create_motif("GGGCCCCC", name = "unrelated")
merged <- merge_similar2(list(m1, m2, m3, m4), qvalue = 0.05)
length(merged)  # 2: one merged + the unrelated singleton
}

}
\seealso{
\code{\link[=merge_similar]{merge_similar()}}, \code{\link[=merge_motifs2]{merge_motifs2()}}, \code{\link[=compare_motifs2]{compare_motifs2()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
