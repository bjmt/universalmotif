% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_sequences2.R
\name{scan_sequences2}
\alias{scan_sequences2}
\title{Minimalist motif scanner aligned with \verb{yamtk scan} defaults.}
\usage{
scan_sequences2(motifs, sequences, pvalue = 1e-04, RC = TRUE,
  nthreads = 1, return.granges = NULL, no.overlaps = FALSE,
  no.overlaps.by = c("pvalue", "score"), no.overlaps.by.strand = FALSE,
  no.overlaps.by.motif = FALSE)
}
\arguments{
\item{motifs}{See \code{\link[=convert_motifs]{convert_motifs()}} for accepted motif formats. DNA or RNA
only -- amino-acid and custom alphabet motifs are rejected.}

\item{sequences}{\code{XStringSet}. DNA or RNA sequences to scan; the alphabet
must match the motif alphabet.}

\item{pvalue}{\code{numeric(1)}. P-value cutoff for reporting hits. Default
\code{1e-4}.}

\item{RC}{\code{logical(1)}. If \code{TRUE} (default), scan both strands.}

\item{nthreads}{\code{numeric(1)}. Number of threads. \code{nthreads = 0} uses all
available threads. Parallelisation happens inside \code{scan_sequences_cpp}
over the motif x sequence cross-product.}

\item{return.granges}{\code{logical(1)} or \code{NULL}. When \code{NULL} (default), returns
a \code{GRanges} if the \code{GenomicRanges} package is installed, otherwise a
\code{data.frame}. Set \code{TRUE} / \code{FALSE} to force one or the other.}

\item{no.overlaps}{\code{logical(1)}. If \code{TRUE}, drop overlapping hits within
each \verb{(sequence, motif, strand)} group using a greedy
algorithm (see \code{\link[=dedup_hits]{dedup_hits()}}). Default \code{FALSE}.}

\item{no.overlaps.by}{\code{character(1)}. Which column to use as the priority
for breaking ties between overlapping hits. \code{"pvalue"} (default) keeps
the lowest p-value; \code{"score"} keeps the highest log-odds score. Only
used when \code{no.overlaps = TRUE}.}

\item{no.overlaps.by.strand}{\code{logical(1)}. If \code{TRUE}, overlapping hits on
opposite strands compete with each other. If \code{FALSE} (default), each
strand is deduplicated independently.}

\item{no.overlaps.by.motif}{\code{logical(1)}. If \code{TRUE}, overlapping hits from
different motifs compete with each other. If \code{FALSE} (default), each
motif is deduplicated independently.}
}
\value{
Either a \code{GRanges} or a \code{data.frame}.

Common fields (both shapes):
\code{motif}, \code{motif.i}, \code{sequence.i}, \code{score}, \code{score.pct}, \code{match}, \code{pvalue}.

Coordinate fields:
\itemize{
\item \code{GRanges}: \code{seqnames} (sequence name), \code{start}, \code{end}, \code{strand}.
\item \code{data.frame}: \code{sequence}, \code{start}, \code{end}, \code{strand}.
}

Coordinates are always 1-based, inclusive, with \code{start <= end} regardless of
strand. For hits on the \code{-} strand the \code{match} column is the reverse
complement of the sequence substring; i.e. the sequence as matched against
the motif.
}
\description{
\code{scan_sequences2()} is a deliberately stripped-down counterpart to
\code{\link[=scan_sequences]{scan_sequences()}} whose default surface mirrors the command-line tool
\href{https://github.com/bjmt/yamtk}{yamtk}.
It exposes a single threshold (a P-value),
always scans both strands by default, always computes per-hit P-values,
and returns either a \code{GRanges} (preferred when GenomicRanges is
installed) or a \code{data.frame}. Use \code{\link[=scan_sequences]{scan_sequences()}} when you need any of
multifreq scoring, gapped motifs, q-values, exhaustive P-values, \code{respect.strand},
\code{allow.nonfinite}, or \code{threshold.type}s other than P-value.
}
\details{
The scanner re-uses the same C++ core as
\code{\link[=scan_sequences]{scan_sequences()}}; the speed advantage comes from skipping the R-side
bookkeeping for features this function does not support.

P-values are computed via the dynamic-programming algorithm of
\code{\link[=motif_pvalue]{motif_pvalue()}} (FIMO-style; Grant et al. 2011), which is also what
\verb{yamtk scan} uses.
}
\examples{
library(universalmotif)
motifs <- create_motif(c("TATAAA", "CACGTG"))
seqs <- create_sequences(seqnum = 5, seqlen = 200, rng.seed = 1)
hits <- scan_sequences2(motifs, seqs, pvalue = 1e-3, return.granges = FALSE)
head(hits)

}
\references{
Grant CE, Bailey TL, Noble WS (2011). "FIMO: scanning for occurrences of a
given motif." \emph{Bioinformatics}, \strong{27}(7), 1017-1018.

Tremblay BJM (2026). yamtk: Yet Another Motif ToolKit.
\url{https://github.com/bjmt/yamtk}.
}
\seealso{
\code{\link[=scan_sequences]{scan_sequences()}}, \code{\link[=motif_pvalue]{motif_pvalue()}}, \code{\link[=convert_motifs]{convert_motifs()}}
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@uwaterloo.ca}
}
